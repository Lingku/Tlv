language: cpp
sudo: required
compiler:
  - gcc

before_install:
  - sudo bash travis/get_cmake.sh
  - sudo apt-get install -qq

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - pwd
  - make